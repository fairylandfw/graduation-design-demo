# 网络舆情分析平台 - 部署指南

## 环境准备

### 1. 安装依赖软件
- Python 3.8+
- Node.js 14+
- MySQL 5.7+
- Redis 5.0+

### 2. 数据库初始化

```sql
CREATE DATABASE sentiment_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

## 后端部署

### 1. 安装Python依赖
```bash
cd backend
pip install -r requirements.txt
```

### 2. 配置环境变量
```bash
cp .env.example .env
# 编辑.env文件，配置数据库和Redis连接信息
```

### 3. 初始化数据库
```bash
python -c "from app import create_app, db; app = create_app(); app.app_context().push(); db.create_all()"
```

### 4. 启动后端服务
```bash
python run.py
```

后端服务将在 http://localhost:5000 启动

## 前端部署

### 1. 安装依赖
```bash
cd frontend
npm install
```

### 2. 启动开发服务器
```bash
npm run serve
```

前端应用将在 http://localhost:8080 启动

### 3. 生产环境构建
```bash
npm run build
```

## 数据采集

### 运行爬虫
```bash
cd backend/crawler
scrapy crawl news_spider -a urls="http://example.com/news"
```

### 定时任务
可以使用APScheduler或系统cron配置定时采集任务

## 功能使用

### 1. 情感分析
- 访问首页查看情感分布饼图
- 支持正面、中性、负面三分类
- 基于词典分析，可解释性强

### 2. 热点发现
- 查看热词云图
- 支持TF-IDF关键词提取
- 话题聚类分析

### 3. 趋势分析
- 查看情感趋势折线图
- 支持自定义时间范围
- 多维度数据筛选

### 4. 预警功能
- 负面舆情激增预警
- 热点话题预警
- 自动告警通知

### 5. 报告生成
- 一键生成Excel分析报告
- 包含概览、情感分析、热点话题等内容

## 扩展开发

### 添加新的数据源
1. 在 `backend/crawler/spiders/` 创建新的爬虫文件
2. 继承 `scrapy.Spider` 类
3. 实现 `parse` 方法

### 自定义情感词典
编辑以下文件：
- `data/dictionaries/positive.txt` - 正面词
- `data/dictionaries/negative.txt` - 负面词
- `data/dictionaries/degree.txt` - 程度副词
- `data/dictionaries/negation.txt` - 否定词

### 调整预警阈值
编辑 `backend/config/config.py`：
```python
ALERT_NEGATIVE_RATIO = 0.6  # 负面比例阈值
ALERT_HOTSPOT_THRESHOLD = 100  # 热点阈值
```

## 常见问题

### 1. 数据库连接失败
检查MySQL服务是否启动，确认连接配置正确

### 2. Redis连接失败
检查Redis服务是否启动，确认端口配置

### 3. 爬虫被封禁
- 增加请求延迟
- 使用代理IP
- 遵守robots.txt规则

### 4. 分词效果不佳
- 添加自定义词典
- 使用领域专用分词工具

## 性能优化

### 1. 数据库优化
- 为常用查询字段添加索引
- 定期清理历史数据

### 2. 缓存优化
- 使用Redis缓存热点数据
- 设置合理的过期时间

### 3. 并发优化
- 使用Gunicorn部署后端
- 配置Nginx反向代理

## 安全建议

1. 修改默认密钥
2. 使用HTTPS协议
3. 实施访问控制
4. 定期备份数据
5. 监控异常访问

## 技术支持

如遇问题，请查看项目文档或提交Issue
